# Captured data — subject images and video stay off the repo
recordings/
*.mp4
*.mkv
*.png
*.tif
*.tiff
*.raw

# Python virtual environment (recreate with the command in README.md)
venv/
__pycache__/
*.py[cod]

# Stabilization results and fetched reference bursts — both regenerable
# (results from the recordings, bursts with tools/fetch_reference_data.py)
stabilization/
reference_data/burst_*/
_work/
